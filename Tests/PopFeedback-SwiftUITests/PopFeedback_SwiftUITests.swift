import XCTest
@testable import PopFeedback_SwiftUI

final class PopFeedback_SwiftUITests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PopFeedback_SwiftUI().text, "Hello, World!")
    }
}
